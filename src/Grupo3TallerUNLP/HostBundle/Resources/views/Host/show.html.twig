{% extends '::dashboard.html.twig' %}

{% block content -%}
    <h1>Host</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dirección IP</th>
                <td>{{ entity.ipAddress }}</td>
            </tr>
            <tr>
                <th>Networkusers</th>
                <td>{{ entity.networkUsers }}</td>
            </tr>
            <tr>
                <th>Office</th>
                <td>{{ entity.office }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('grupo3_taller_unlp_admin_host') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('grupo3_taller_unlp_admin_host_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
